spring:
  application:
    name: eventManager
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
  profiles: 
    active:
    - dev
    
server:
  port: 8090
  
logging:
  level: 
    org.jeavio.meetin.eventManager: DEBUG